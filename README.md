This is a backend application which communicates purely through REST APIs. The TechStack is Java Springboot.
Product, Order and Order Item entities along with DTOs were implemented.
Pagination, Filter searching features are provided.
Coded a default seeder to inject default data into the database.
A clean Controller layer, Service layer and Repository Layer are implemented and their interfaces and applications are well defined.
